import { PLATFORM_OWNER_PREFIX } from "../constants";

export const platformRoutes = {
  dashboard: `${PLATFORM_OWNER_PREFIX}/dashboard`,

  tenants: `${PLATFORM_OWNER_PREFIX}/tenants`,

  packages: `${PLATFORM_OWNER_PREFIX}/packages`,

  subscriptions: `${PLATFORM_OWNER_PREFIX}/subscriptions`,

  billing: `${PLATFORM_OWNER_PREFIX}/billing`,

  platform: `${PLATFORM_OWNER_PREFIX}/platform`,

  accessControl: `${PLATFORM_OWNER_PREFIX}/access-control`,

  reports: `${PLATFORM_OWNER_PREFIX}/reports`,

  support: `${PLATFORM_OWNER_PREFIX}/support`,

  system: `${PLATFORM_OWNER_PREFIX}/system`,

  integrations: `${PLATFORM_OWNER_PREFIX}/integrations`,

  settings: `${PLATFORM_OWNER_PREFIX}/settings`,
};
