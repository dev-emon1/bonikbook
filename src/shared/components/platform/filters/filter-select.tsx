import { AppSelect } from "@/shared/ui/app-select";
import type { AppSelectProps } from "@/shared/ui/app-select";

export function FilterSelect(props: AppSelectProps) {
  return <AppSelect {...props} />;
}
